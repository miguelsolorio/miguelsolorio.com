{
  "name": "hugo-tailwind",
  "version": "1.0.0",
  "description": "",
  "author": "miguelsolorio",
  "license": "MIT",
  "scripts": {
    "develop": "hugo server -D --bind 0.0.0.0 --port 1313 --forceSyncStatic --noHTTPCache",
    "export": "hugo --minify --cleanDestinationDir",
    "update:metrics": "node scripts/update-project-metrics.mjs",
    "build:resume": "node scripts/build-resume.mjs"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.13",
    "hugo-bin": "^0.111.3",
    "postcss": "^8.4.20",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.2.4"
  }
}
